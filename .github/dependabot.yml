version: 2
updates:
- package-ecosystem: github-actions
  directory: /
  schedule:
    interval: monthly
  commit-message:
    prefix: 'ci(dependabot):'
  labels:
  - ci
  - dependencies
- package-ecosystem: uv
  directory: /
  schedule:
    interval: monthly
  commit-message:
    prefix: 'ci(dependabot):'
  labels:
  - ci
  - dependencies
  allow:
  - dependency-name: sionna-rt
